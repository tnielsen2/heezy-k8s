apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: APP_NAME-config
  namespace: heezy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
